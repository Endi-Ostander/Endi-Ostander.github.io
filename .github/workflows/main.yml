name: Build Cordova APK

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 40

    steps:
      # 1️⃣ Клонируем репозиторий
      - uses: actions/checkout@v4

      # 2️⃣ Просматриваем корень репозитория
      - name: List repo files
        run: ls -la

      # 3️⃣ Настраиваем Java 17
      - name: Set up Java 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      # 4️⃣ Настраиваем Node.js (для Cordova CLI)
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      # 5️⃣ Устанавливаем Cordova CLI
      - name: Install Cordova CLI
        run: npm install -g cordova

      # 6️⃣ Проверяем директорию проекта
      - name: Check Cordova project directory
        run: |
          if [ ! -d "cordova-project" ]; then
            echo "ERROR: 'cordova-project' folder not found in repo root!"
            exit 1
          fi

      - name: Move into project directory
        run: cd cordova-project
        shell: bash

      # 7️⃣ Устанавливаем Android SDK/NDK
      - name: Install Android SDK and NDK
        run: |
          sudo apt-get update
          sudo apt-get install -y unzip wget

          export ANDROID_HOME=$HOME/android-sdk
          mkdir -p $ANDROID_HOME/cmdline-tools
          cd $ANDROID_HOME/cmdline-tools

          # Скачиваем и распаковываем командные инструменты
          wget -q https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip
          unzip -q commandlinetools-linux-9477386_latest.zip -d tmp
          mv tmp/cmdline-tools latest
          rm -rf tmp commandlinetools-linux-9477386_latest.zip

          # Добавляем пути в окружение
          echo "ANDROID_HOME=$ANDROID_HOME" >> $GITHUB_ENV
          echo "ANDROID_SDK_ROOT=$ANDROID_HOME" >> $GITHUB_ENV
          echo "$ANDROID_HOME/cmdline-tools/latest/bin:$ANDROID_HOME/platform-tools" >> $GITHUB_PATH

          # Принимаем лицензии и устанавливаем платформы
          yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --licenses
          $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager "platform-tools" "platforms;android-34" "build-tools;34.0.0"

          # Устанавливаем NDK
          $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager "ndk;25.2.9519653"
          echo "ANDROID_NDK_HOME=$ANDROID_HOME/ndk/25.2.9519653" >> $GITHUB_ENV

      # 8️⃣ Устанавливаем npm-зависимости проекта
      - name: Install npm dependencies
        run: |
          cd cordova-project
          if [ -f "package.json" ]; then
            npm install
          else
            echo "No package.json found. Skipping npm install."
          fi

      # 9️⃣ Добавляем платформу Android (если еще нет)
      - name: Add Android platform
        run: |
          cd cordova-project
          if ! cordova platform list | grep -q android; then
            cordova platform add android
          fi

      # 🔟 Собираем релиз APK
      - name: Build Android Release APK
        run: |
          cd cordova-project
          cordova build android --release

      # 1️⃣1️⃣ Загружаем APK как артефакт
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: cordova-apk
          path: cordova-project/platforms/android/app/build/outputs/apk/release/app-release.apk
          if-no-files-found: error
          retention-days: 5
